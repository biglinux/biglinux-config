#!/usr/bin/env bash
set -euo pipefail

# BigLinux Config — GTK4/Adwaita application
# Launches the Python app for restoring application settings

APP_DIR="/usr/share/biglinux/biglinux-config"

exec python3 "${APP_DIR}/main.py" "$@"
